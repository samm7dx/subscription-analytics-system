# Server
PORT=5000
NODE_ENV=development

# CORS — frontend URL (local + production)
FRONTEND_URL=http://localhost:5173

# MySQL (local or Railway)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=subscription_analytics_system

# Railway MySQL: set DB_SSL=true
DB_SSL=false
